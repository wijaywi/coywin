[package]
name = "coywin-client"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
coywin-zksteg = { path = "../coywin-zksteg" }
wasm-bindgen = "0.2"
halo2_proofs = { version = "0.3.0", default-features = false }
halo2curves = "0.1.0"
ff = "0.13"
getrandom = { version = "0.2", features = ["js"] }
